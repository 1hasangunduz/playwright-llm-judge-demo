{
  "name": "playwright-llm-judge-demo",
  "version": "0.2.0",
  "private": true,
  "description": "Companion repo for the Medium article: LLM-as-Judge with Playwright and Gemini 2.5",
  "scripts": {
    "test": "playwright test",
    "test:local": "RUN_AGAINST_LOCAL=true playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@google/genai": "^1.52.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  }
}
