{
  "name": "playwright-llm-judge-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Companion repo for the Medium article: LLM-as-Judge with Playwright and GPT-4o",
  "scripts": {
    "test": "playwright test",
    "test:local": "RUN_AGAINST_LOCAL=true playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.68.0",
    "zod": "^3.23.8"
  }
}
